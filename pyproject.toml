[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "lgpd-helper-rag"
version = "0.1.0"
description = "Assistente RAG para dúvidas de compliance LGPD"
requires-python = ">=3.10"
dependencies = [
    "streamlit",
    "python-dotenv",
    "pypdf",
    "chromadb",
    "google-generativeai",
    "numpy",
    "pandas",
    "pytest",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
